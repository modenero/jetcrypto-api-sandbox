version: "3"
services:
  web:
    build: .
    ports:
    - '3000:3000'
    volumes:
    - ./public:/var/www/html
    - logs:/var/log
    links:
    - redis
  redis:
    image: redis
    volumes:
      logs: {}
